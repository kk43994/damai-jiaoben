# 红手指云手机使用指南

## 目录
1. [快速开始](#快速开始)
2. [获取ADB连接地址](#获取adb连接地址)
3. [使用方法](#使用方法)
4. [常见问题](#常见问题)
5. [进阶功能](#进阶功能)

---

## 快速开始

### 第一步：获取红手指ADB地址

1. 登录红手指官网：https://www.gc.com.cn/
2. 进入"我的云手机"
3. 找到你要使用的云手机
4. 查看ADB连接信息（格式：`IP:端口`）
   - 例如：`127.0.0.1:58358`

### 第二步：连接设备

**方式1：命令行快速连接（推荐）**

```bash
# 直接连接（自动添加设备）
python hongshouzhi_connect.py 127.0.0.1:58358

# 成功输出：
# [OK] 已添加设备: 红手指_58358
# [OK] 成功连接到 红手指_58358
```

**方式2：交互式管理**

```bash
# 启动管理工具
python hongshouzhi_connect.py

# 按提示操作：
# 1. 列出所有设备
# 2. 添加红手指设备
# 3. 连接设备
# ...
```

### 第三步：验证连接

```bash
# 检查设备是否连接
adb devices

# 应该看到：
# 127.0.0.1:58358    device
```

### 第四步：开始抢票

```bash
# 进入抢票目录
cd damai_appium

# 编辑配置文件
notepad config.jsonc

# 运行抢票程序
python damai_app_v2.py
```

---

## 获取ADB连接地址

### 红手指官方平台

#### 方法1：Web控制台
1. 访问 https://www.gc.com.cn/
2. 登录账号
3. 进入"我的云手机"
4. 点击云手机卡片
5. 查看"ADB调试"或"连接信息"

#### 方法2：红手指客户端
1. 打开红手指PC客户端
2. 右键点击云手机
3. 选择"连接信息"或"ADB调试"
4. 复制ADB地址

### ADB地址格式

正确格式：
```
127.0.0.1:58358
```

错误格式：
```
❌ 58358              # 缺少IP
❌ 127.0.0.1         # 缺少端口
❌ localhost:58358   # 应使用127.0.0.1
```

---

## 使用方法

### 1. 红手指连接工具 (hongshouzhi_connect.py)

#### 命令行模式

```bash
# 快速连接
python hongshouzhi_connect.py 127.0.0.1:58358

# 连接成功后自动保存设备信息
```

#### 交互式菜单

```bash
python hongshouzhi_connect.py
```

菜单选项：
```
1. 列出所有设备        # 查看已保存的设备
2. 添加红手指设备      # 添加新设备
3. 连接设备           # 连接到已保存的设备
4. 断开设备           # 断开已连接的设备
5. 删除设备           # 删除设备记录
6. 检查所有设备状态    # 批量检查状态
7. 自动检测新设备      # 扫描系统中的设备
0. 退出
```

#### 示例：添加设备

```
请选择操作 (0-7): 2

------------------------------------------------------------
添加红手指设备
------------------------------------------------------------

请输入设备信息:
  设备名称 (例: 红手指1): 我的红手指1
  ADB地址 (格式: IP:端口, 例: 127.0.0.1:58358): 127.0.0.1:58358

[OK] 设备已添加: 我的红手指1

是否立即连接此设备? (y/n): y

正在连接 我的红手指1...
[OK] 成功连接到 我的红手指1
```

### 2. 设备管理器 (device_manager.py)

#### Python代码示例

```python
from damai_appium.device_manager import DeviceManager

# 创建管理器
manager = DeviceManager("devices.json")

# 添加设备
device = manager.add_device(
    name="红手指1",
    address="127.0.0.1:58358",
    device_type="hongshouzhi"
)

# 连接设备
success = manager.connect_device(device)
if success:
    print(f"连接成功！当前设备: {manager.current_device.name}")

# 列出所有设备
for device in manager.list_devices():
    print(f"{device.name}: {device.status}")

# 检查设备状态
status = manager.check_device_status(device)
print(f"设备状态: {status}")

# 自动检测新设备
new_devices = manager.auto_detect_devices()
print(f"检测到 {len(new_devices)} 个新设备")

# 断开设备
manager.disconnect_device(device)
```

---

## 常见问题

### Q1: 连接失败：already connected

**问题**：
```
already connected to 127.0.0.1:58358
```

**解决**：
这其实表示连接成功！程序会继续验证设备状态。

---

### Q2: 连接失败：cannot connect

**问题**：
```
[ERROR] 连接失败: cannot connect to 127.0.0.1:58358
```

**可能原因**：
1. ❌ 红手指云手机未启动
2. ❌ ADB端口错误
3. ❌ 防火墙阻止连接
4. ❌ 红手指客户端未开启ADB调试

**解决方案**：
```bash
# 1. 检查红手指是否在线
# 登录红手指官网查看设备状态

# 2. 重新获取ADB地址
# 端口可能会变化，重新复制最新的地址

# 3. 测试端口连通性
telnet 127.0.0.1 58358

# 4. 重启红手指客户端
# 关闭并重新打开红手指PC客户端
```

---

### Q3: 设备显示offline

**问题**：
```
127.0.0.1:58358    offline
```

**解决**：
```bash
# 1. 重新连接
adb disconnect 127.0.0.1:58358
adb connect 127.0.0.1:58358

# 2. 重启ADB服务
adb kill-server
adb start-server
adb connect 127.0.0.1:58358

# 3. 在红手指云手机上允许USB调试
# 首次连接时会弹出授权对话框
```

---

### Q4: 端口号变化了

**问题**：
红手指重启后ADB端口变了

**解决**：
```bash
# 方法1: 重新添加设备
python hongshouzhi_connect.py 127.0.0.1:新端口

# 方法2: 使用交互式工具
python hongshouzhi_connect.py
# 选择 5. 删除设备（删除旧端口）
# 选择 2. 添加红手指设备（添加新端口）
```

---

### Q5: 多个红手指设备如何管理？

**解决**：
```python
from damai_appium.device_manager import DeviceManager

manager = DeviceManager("devices.json")

# 添加多个设备
manager.add_device("红手指1", "127.0.0.1:58358", "hongshouzhi")
manager.add_device("红手指2", "127.0.0.1:59700", "hongshouzhi")
manager.add_device("红手指3", "127.0.0.1:62001", "hongshouzhi")

# 批量连接
for device in manager.list_devices():
    print(f"连接 {device.name}...")
    manager.connect_device(device)

# 切换当前设备
device2 = manager.get_device("红手指2")
manager.connect_device(device2)
print(f"当前设备: {manager.current_device.name}")
```

---

## 进阶功能

### 1. 批量管理多台红手指

创建脚本 `batch_connect.py`：

```python
from damai_appium.device_manager import DeviceManager

# 红手指设备列表
HONGSHOUZHI_DEVICES = [
    ("红手指1", "127.0.0.1:58358"),
    ("红手指2", "127.0.0.1:59700"),
    ("红手指3", "127.0.0.1:62001"),
]

manager = DeviceManager("devices.json")

# 批量添加
for name, address in HONGSHOUZHI_DEVICES:
    try:
        device = manager.add_device(name, address, "hongshouzhi")
        print(f"[OK] 添加: {name}")
    except ValueError as e:
        print(f"[SKIP] {name}: {e}")

# 批量连接
for name, _ in HONGSHOUZHI_DEVICES:
    device = manager.get_device(name)
    if device:
        if manager.connect_device(device):
            print(f"[OK] 连接: {name}")
        else:
            print(f"[FAIL] 连接失败: {name}")

# 检查所有设备
print("\n设备状态:")
for device in manager.list_devices():
    print(f"  {device.name}: {device.status}")
```

运行：
```bash
python batch_connect.py
```

---

### 2. 自动重连脚本

创建 `auto_reconnect.py`：

```python
import time
from damai_appium.device_manager import DeviceManager

def auto_reconnect(device_name, max_retries=5, retry_interval=10):
    """自动重连设备"""
    manager = DeviceManager("devices.json")
    device = manager.get_device(device_name)

    if not device:
        print(f"[ERROR] 设备不存在: {device_name}")
        return False

    for attempt in range(1, max_retries + 1):
        print(f"\n[尝试 {attempt}/{max_retries}] 连接 {device_name}...")

        if manager.connect_device(device):
            print(f"[OK] 连接成功！")
            return True

        if attempt < max_retries:
            print(f"[WAIT] {retry_interval}秒后重试...")
            time.sleep(retry_interval)

    print(f"[FAIL] 连接失败，已达到最大重试次数")
    return False

# 使用示例
auto_reconnect("红手指1", max_retries=5, retry_interval=10)
```

---

### 3. 与配置文件集成

更新 `damai_appium/config.jsonc`：

```jsonc
{
  "server_url": "http://127.0.0.1:4723",
  "adb_port": "58358",  // 红手指端口

  // 设备配置
  "device": {
    "name": "红手指1",
    "address": "127.0.0.1:58358",
    "type": "hongshouzhi",
    "auto_connect": true  // 自动连接
  },

  "keyword": "演出关键词",
  "city": "北京",
  "date": "11.2",
  "price": "50",
  // ...
}
```

在 `damai_app_v2.py` 中添加：

```python
from damai_appium.device_manager import DeviceManager

class DamaiBot:
    def __init__(self):
        # 加载配置
        self.config = Config()

        # 自动连接设备
        if self.config.data.get('device', {}).get('auto_connect'):
            self._auto_connect_device()

        # 初始化驱动
        self._setup_driver()

    def _auto_connect_device(self):
        """自动连接设备"""
        device_config = self.config.data.get('device', {})
        device_address = device_config.get('address')

        if not device_address:
            return

        print(f"自动连接设备: {device_address}")
        manager = DeviceManager("devices.json")

        # 查找或添加设备
        device = None
        for d in manager.list_devices():
            if d.address == device_address:
                device = d
                break

        if not device:
            device = manager.add_device(
                name=device_config.get('name', 'Auto'),
                address=device_address,
                device_type=device_config.get('type', 'hongshouzhi')
            )

        # 连接设备
        manager.connect_device(device)
```

---

### 4. 设备健康监控

创建 `device_monitor.py`：

```python
import time
from datetime import datetime
from damai_appium.device_manager import DeviceManager

def monitor_devices(interval=60):
    """监控设备状态"""
    manager = DeviceManager("devices.json")

    print("开始监控设备状态...")
    print(f"检查间隔: {interval}秒\n")

    while True:
        timestamp = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
        print(f"\n[{timestamp}] 检查设备状态:")
        print("-" * 60)

        for device in manager.list_devices():
            old_status = device.status
            new_status = manager.check_device_status(device)

            # 状态变化时重新连接
            if new_status != "connected" and old_status == "connected":
                print(f"  [ALERT] {device.name}: {old_status} -> {new_status}")
                print(f"  [ACTION] 尝试重新连接...")
                if manager.connect_device(device):
                    print(f"  [OK] 重新连接成功")
                else:
                    print(f"  [FAIL] 重新连接失败")
            else:
                status_icon = "✓" if new_status == "connected" else "✗"
                print(f"  {status_icon} {device.name}: {new_status}")

        print(f"\n下次检查: {interval}秒后")
        time.sleep(interval)

# 每60秒检查一次
monitor_devices(interval=60)
```

---

## 配置文件说明

### devices.json

设备配置文件自动生成，格式：

```json
{
  "devices": [
    {
      "name": "红手指1",
      "address": "127.0.0.1:58358",
      "device_type": "hongshouzhi",
      "status": "connected",
      "last_connected": "2025-11-06T12:34:56",
      "error_message": null
    }
  ],
  "current_device": "红手指1"
}
```

字段说明：
- `name`: 设备名称（用户自定义）
- `address`: ADB地址（IP:端口）
- `device_type`: 设备类型（hongshouzhi/emulator/local/cloud）
- `status`: 当前状态（connected/disconnected/offline/error）
- `last_connected`: 最后连接时间
- `error_message`: 错误信息（如果有）
- `current_device`: 当前活动设备

---

## 技术原理

### ADB连接流程

```
1. 用户输入: 127.0.0.1:58358
   ↓
2. DeviceManager.connect_device()
   ↓
3. 执行: adb connect 127.0.0.1:58358
   ↓
4. 验证: adb devices
   ↓
5. 检查: 127.0.0.1:58358  device
   ↓
6. 状态: connected
   ↓
7. 保存到: devices.json
```

### 设备状态机

```
disconnected (未连接)
    ↓ connect_device()
connecting (连接中)
    ↓ 成功
connected (已连接) ←→ offline (离线)
    ↓ disconnect_device()
disconnected (未连接)

    ↓ 失败
error (错误)
```

---

## 最佳实践

### 1. 命名规范

建议使用有意义的设备名称：

```python
# ✓ 好的命名
"红手指-抢票专用1"
"红手指-备用设备"
"红手指-测试机"

# ✗ 不好的命名
"设备1"
"test"
"aaa"
```

### 2. 定期检查

建议每次抢票前检查设备状态：

```bash
python hongshouzhi_connect.py
# 选择: 6. 检查所有设备状态
```

### 3. 备份配置

定期备份 `devices.json`：

```bash
# Windows
copy devices.json devices_backup.json

# Linux/Mac
cp devices.json devices_backup.json
```

### 4. 多设备策略

- **抢票用**: 保持长期连接
- **测试用**: 临时连接，用完断开
- **备用机**: 提前配置，紧急使用

---

## 故障排除

### ADB未找到

```bash
# 检查ADB是否安装
adb version

# 如果未安装，设置环境变量
set ANDROID_HOME=C:\Users\你的用户名\AppData\Local\Android\Sdk
set PATH=%PATH%;%ANDROID_HOME%\platform-tools
```

### 端口被占用

```bash
# 查看端口占用
netstat -ano | findstr 58358

# 结束占用进程
taskkill /F /PID 进程ID
```

### 设备unauthorized

红手指云手机上允许USB调试授权：
1. 在云手机屏幕上查看授权弹窗
2. 点击"允许"或"始终允许"

---

## 更新日志

### v1.0 (2025-11-06)
- ✨ 首次发布
- ✨ 支持红手指设备管理
- ✨ 命令行快速连接
- ✨ 交互式设备管理
- ✨ 自动检测新设备
- ✨ 多设备管理
- ✨ 配置持久化

---

## 联系支持

如有问题，请：
1. 查看本文档的[常见问题](#常见问题)部分
2. 检查 `devices.json` 文件内容
3. 运行环境检测：`python environment_checker.py`
4. 提交Issue到项目仓库

---

**Happy Ticket Grabbing! 🎫**
