# 📘 v2.2.0 新功能使用指南

> **版本**: v2.2.0
> **更新日期**: 2025-11-17
> **适用对象**: 开发者和高级用户

---

## 🎯 概述

v2.2.0 版本新增了5个强大的核心功能模块，全面提升抢票成功率和系统稳定性。

本指南将教您如何使用这些新功能。

---

## 🚀 快速开始

### 方式一：在现有代码中使用（推荐）

直接在抢票脚本中导入并使用新功能模块。

### 方式二：等待GUI集成版本

等待 v2.2.1 版本，所有功能将集成到图形界面。

---

## 📚 功能使用教程

### 1. **多策略抢票系统**

#### 基础用法

```python
from damai_appium.ticket_strategy import TicketStrategy, StrategyType

# 创建策略管理器
strategy_manager = TicketStrategy(driver, logger=BotLogger)

# 定义多个策略（优先级从高到低）
strategies = [
    strategy_manager.click_by_text("立即预定", partial=False),  # 策略1: 精确文字
    strategy_manager.click_by_text("预定", partial=True),       # 策略2: 模糊文字
    strategy_manager.click_by_coordinate(360, 1200)             # 策略3: 坐标
]

strategy_types = [
    StrategyType.UIAUTOMATOR,
    StrategyType.UIAUTOMATOR,
    StrategyType.COORDINATE
]

# 执行多策略（自动降级和重试）
result = strategy_manager.execute_with_retry(
    strategies,
    strategy_types,
    task_name="点击预定按钮"
)

if result.success:
    print(f"成功！使用策略: {result.strategy_type.value}, 重试次数: {result.retry_count}")
else:
    print(f"失败: {result.error_msg}")
```

#### 使用预设策略

```python
from damai_appium.ticket_strategy import StrategyPresets

# 点击按钮的预设策略组合
strategy_manager, strategies, types = StrategyPresets.click_button_strategies(
    driver,
    button_text="立即预定",
    x=360, y=1200  # 可选：提供坐标作为备用策略
)

result = strategy_manager.execute_with_retry(strategies, types)
```

#### 查看统计

```python
# 打印策略统计信息
strategy_manager.print_statistics()

# 获取统计数据
stats = strategy_manager.get_statistics()
print(f"成功率: {stats['success_rate']:.1f}%")
```

---

### 2. **全局异常处理系统**

#### 基础用法

```python
from damai_appium.error_handler import GlobalErrorHandler, ErrorCategory

# 创建错误处理器
error_handler = GlobalErrorHandler(logger=BotLogger)

# 手动处理错误
try:
    element = driver.find_element(...)
    element.click()
except Exception as e:
    recovered = error_handler.handle_error(
        e,
        context="点击按钮",
        auto_recover=True
    )

    if not recovered:
        print("无法自动恢复，需要人工介入")
```

#### 注册恢复策略

```python
from damai_appium.error_handler import (
    restart_app_strategy,
    scroll_page_strategy,
    reconnect_driver_strategy
)

# 策略1: App崩溃时重启
error_handler.register_recovery_strategy(
    ErrorCategory.APPIUM,
    restart_app_strategy(driver, "cn.damai")
)

# 策略2: 元素未找到时滚动页面
error_handler.register_recovery_strategy(
    ErrorCategory.ELEMENT_NOT_FOUND,
    scroll_page_strategy(driver, direction="down")
)

# 策略3: 连接断开时重连
def reconnect():
    # 重新连接driver的代码
    pass

error_handler.register_recovery_strategy(
    ErrorCategory.NETWORK,
    reconnect_driver_strategy(reconnect)
)
```

#### 安全执行函数

```python
# 安全执行（捕获所有异常）
result = error_handler.safe_execute(
    lambda: driver.find_element(By.XPATH, "//button[@text='确认']"),
    error_context="查找确认按钮",
    fallback_value=None,  # 失败时返回None
    raise_on_error=False  # 不抛出异常
)

if result:
    result.click()
```

#### 查看错误统计

```python
# 打印错误统计
error_handler.statistics.print_summary()

# 获取统计数据
summary = error_handler.statistics.get_summary()
print(f"总错误数: {summary['total_errors']}")
print(f"恢复成功率: {summary['recovery_rate']:.1f}%")
```

---

### 3. **声音提示系统**

#### 基础用法

```python
from damai_appium.sound_notifier import get_sound_notifier

# 获取声音通知器（单例）
sound = get_sound_notifier(enabled=True)

# 播放预定义音效
sound.play_success()         # 成功音
sound.play_error()          # 错误音
sound.play_warning()        # 警告音
sound.play_info()           # 提示音
sound.play_countdown()      # 倒计时音
sound.play_ticket_grabbed() # 抢到票音效
```

#### 在抢票流程中使用

```python
# 开始抢票
sound.play_info()
print("开始抢票...")

try:
    # 抢票逻辑...
    if ticket_grabbed:
        sound.play_ticket_grabbed()  # 成功音效
        print("抢票成功！")
    else:
        sound.play_warning()
        print("抢票失败")

except Exception as e:
    sound.play_error()  # 错误音效
    print(f"发生错误: {e}")
```

#### 控制开关

```python
# 禁用声音
sound.disable()

# 启用声音
sound.enable()

# 切换声音（返回新状态）
is_enabled = sound.toggle()
print(f"声音{'已启用' if is_enabled else '已禁用'}")
```

#### Windows自定义音序列

```python
from damai_appium.sound_notifier import SoundSequences

# 播放预定义音序列
sound.play_custom_sequence(SoundSequences.TICKET_SUCCESS)  # 抢票成功
sound.play_custom_sequence(SoundSequences.TICKET_FAIL)     # 抢票失败
sound.play_custom_sequence(SoundSequences.COUNTDOWN_5SEC)  # 倒计时5秒

# 自定义音序列 [(频率, 持续时间), ...]
custom_sequence = [
    (1000, 100),  # 1000Hz, 100ms
    (1200, 100),
    (1400, 200)
]
sound.play_custom_sequence(custom_sequence)
```

---

### 4. **配置模板管理系统**

#### 基础用法

```python
from damai_appium.config_templates import get_template_manager, TicketConfig

# 获取模板管理器（单例）
manager = get_template_manager()

# 创建配置
config = TicketConfig(
    name="周杰伦演唱会-北京",
    keyword="周杰伦",
    city="北京",
    date="12月31日",
    price="680",
    price_index=0,
    users=["张三", "李四"],
    adb_port="59700",
    server_url="http://127.0.0.1:4723",
    if_commit_order=False,
    notes="跨年演唱会"
)

# 保存模板
manager.save_template(config)
```

#### 加载和使用模板

```python
# 加载模板
config = manager.load_template("周杰伦演唱会-北京")

if config:
    print(f"演出: {config.keyword}")
    print(f"城市: {config.city}")
    print(f"日期: {config.date}")
    print(f"票价: {config.price}")

    # 使用配置创建Bot
    bot = DamaiBot(
        keyword=config.keyword,
        city=config.city,
        date=config.date,
        price=config.price,
        # ... 其他配置
    )
```

#### 模板管理

```python
# 列出所有模板
templates = manager.list_templates()
print(f"共有 {len(templates)} 个模板: {templates}")

# 获取模板信息（用于GUI显示）
templates_info = manager.get_templates_info()
for info in templates_info:
    print(f"{info['name']}: {info['keyword']} ({info['city']}) - {info['updated_at']}")

# 搜索模板
results = manager.search_templates("周杰伦")
print(f"找到 {len(results)} 个相关模板")

# 删除模板
manager.delete_template("旧模板名称")

# 复制模板
manager.duplicate_template(
    source_name="周杰伦演唱会-北京",
    new_name="周杰伦演唱会-上海"
)
```

#### 导入导出

```python
# 导出模板到JSON文件
manager.export_template(
    name="周杰伦演唱会-北京",
    export_path="configs/jay_beijing.json"
)

# 从JSON文件导入模板
manager.import_template(
    import_path="configs/jay_beijing.json",
    overwrite=False  # 不覆盖已存在的模板
)
```

#### 快速创建模板

```python
# 快速创建（使用默认值）
manager.create_quick_template(
    name="五月天-上海",
    keyword="五月天",
    city="上海",
    date="2025-06-01",
    adb_port="59700"
)
```

---

### 5. **抢票倒计时系统**

#### 基础用法

```python
from damai_appium.countdown_timer import CountdownTimer, parse_datetime
from datetime import datetime

# 解析开票时间
target_time = parse_datetime("2025-12-31 20:00:00")
# 或支持多种格式:
# target_time = parse_datetime("12-31 20:00")  # 今年
# target_time = parse_datetime("20:00")        # 今天

# 创建倒计时器
timer = CountdownTimer(
    target_time=target_time,
    prepare_seconds=30,  # 提前30秒准备
    on_countdown_update=update_display,  # 倒计时更新回调
    on_prepare=prepare_grab,             # 准备阶段回调
    on_start=start_grab,                 # 开始抢票回调
    on_complete=complete_callback        # 完成回调
)

# 启动倒计时
timer.start()
```

#### 回调函数示例

```python
def update_display(remaining_seconds):
    """倒计时更新回调"""
    hours = remaining_seconds // 3600
    minutes = (remaining_seconds % 3600) // 60
    seconds = remaining_seconds % 60
    print(f"倒计时: {hours:02d}:{minutes:02d}:{seconds:02d}")

    # 最后10秒播放提示音
    if remaining_seconds <= 10:
        sound.play_countdown()

def prepare_grab():
    """准备阶段回调（提前30秒）"""
    print("准备阶段：加载页面...")
    sound.play_warning()
    # 打开App，加载到演出详情页
    driver.activate_app("cn.damai")
    # ... 导航到详情页

def start_grab():
    """开始抢票回调"""
    print("开始抢票！")
    sound.play_ticket_grabbed()
    # 开始执行抢票逻辑
    bot.start_grab()

def complete_callback():
    """完成回调"""
    print("倒计时结束")
```

#### 控制倒计时

```python
# 获取剩余时间
remaining = timer.get_remaining_seconds()
print(f"剩余 {remaining} 秒")

# 格式化显示
formatted = timer.format_remaining_time()
print(f"剩余时间: {formatted}")  # "01:23:45"

# 检查是否正在运行
if timer.is_active():
    print("倒计时运行中")

# 停止倒计时
timer.stop()
```

#### 多任务管理

```python
from damai_appium.countdown_timer import CountdownManager

# 创建管理器
manager = CountdownManager()

# 添加多个倒计时任务
timer1 = CountdownTimer(target_time=time1, on_start=start_grab1)
timer2 = CountdownTimer(target_time=time2, on_start=start_grab2)

manager.add_timer("演出1", timer1)
manager.add_timer("演出2", timer2)

# 启动所有
timer1.start()
timer2.start()

# 列出所有任务
tasks = manager.list_timers()
print(f"当前任务: {tasks}")

# 停止所有
manager.stop_all()
```

---

## 🔥 完整示例：集成所有新功能

```python
from damai_appium.ticket_strategy import StrategyPresets, TicketStrategy
from damai_appium.error_handler import GlobalErrorHandler, ErrorCategory, restart_app_strategy
from damai_appium.sound_notifier import get_sound_notifier
from damai_appium.config_templates import get_template_manager
from damai_appium.countdown_timer import CountdownTimer, parse_datetime

# 1. 初始化所有模块
sound = get_sound_notifier(enabled=True)
error_handler = GlobalErrorHandler()
template_manager = get_template_manager()

# 2. 注册错误恢复策略
error_handler.register_recovery_strategy(
    ErrorCategory.APPIUM,
    restart_app_strategy(driver, "cn.damai")
)

# 3. 加载配置模板
config = template_manager.load_template("周杰伦演唱会-北京")
if not config:
    print("模板不存在，使用默认配置")
    config = default_config

# 4. 创建抢票倒计时
target_time = parse_datetime("2025-12-31 20:00:00")

def on_start_grab():
    """倒计时结束，开始抢票"""
    sound.play_info()

    # 使用多策略点击预定按钮
    strategy_manager, strategies, types = StrategyPresets.click_button_strategies(
        driver,
        button_text="立即预定",
        x=360, y=1200
    )

    result = error_handler.safe_execute(
        lambda: strategy_manager.execute_with_retry(strategies, types),
        error_context="点击预定按钮",
        fallback_value=None
    )

    if result and result.success:
        sound.play_success()
        print("抢票成功！")
    else:
        sound.play_error()
        print("抢票失败")

timer = CountdownTimer(
    target_time=target_time,
    prepare_seconds=30,
    on_start=on_start_grab
)

# 5. 启动倒计时
timer.start()

# 6. 查看统计
print("\n=== 错误统计 ===")
error_handler.statistics.print_summary()

print("\n=== 策略统计 ===")
strategy_manager.print_statistics()
```

---

## ❓ 常见问题

### Q1: 如何在GUI中使用这些新功能？

**A**: 目前这些是核心模块，需要在代码中使用。等待 v2.2.1 版本，所有功能将集成到GUI界面。

### Q2: 声音在Linux上不工作？

**A**: 确保安装了 `beep` 命令：
```bash
sudo apt-get install beep
```

### Q3: 如何调整重试次数？

**A**: 修改 `RetryConfig`:
```python
strategy_manager.retry_config.max_retries = 5  # 改为5次
strategy_manager.retry_config.retry_delay = 2.0  # 初始延迟2秒
```

### Q4: 配置模板保存在哪里？

**A**: 保存在项目目录的 `config_templates/templates.json` 文件中。

### Q5: 倒计时不够精确？

**A**: 倒计时精度到秒级，最后10秒会提高更新频率到0.1秒。如需更高精度，可修改 `countdown_timer.py`。

---

## 📖 更多资源

- **完整功能说明**: 查看 `V2.2_FEATURES.md`
- **API文档**: 查看各模块文件的文档字符串
- **GitHub Issues**: https://github.com/kk43994/damai-jiaoben/issues

---

## 🎉 开始使用吧！

现在您已经掌握了所有新功能的使用方法，开始享受更高的抢票成功率和更稳定的系统吧！

**祝抢票顺利！** 🎫✨
